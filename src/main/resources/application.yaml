spring:
  profiles:
    active: local
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace

---

spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mysql://localhost:3306/travel?useSSL=false&useUnicode=true
    username: root
    password: ths20712
    driver-class-name: com.mysql.cj.jdbc.Driver